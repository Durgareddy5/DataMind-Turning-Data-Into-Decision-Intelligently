# Read only by docker-compose.yml, for variable substitution when
# provisioning the mysql container. The application's own runtime secrets
# (GEMINI_API_KEY, JWT_SECRET, etc.) live in apps/api/.env, not here.
MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=sales_business
MYSQL_APP_USER=ai_analyst_readonly
MYSQL_APP_PASSWORD=
